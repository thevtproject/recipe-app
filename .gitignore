node_modules
.next
# Environment files — one per environment, never committed
.env.runner
.env
.env.prod
.env.dev
.env.local
public/uploads/*.jpg
public/uploads/*.jpeg
public/uploads/*.png
public/uploads/*.webp
!public/uploads/.gitkeep

# Certbot Cloudflare API token — never commit
certbot/cloudflare.ini

# Cloudflare Tunnel credentials (contain secret tunnel key)
*.tunnel-credentials.json
# UUID-named tunnel credential files
/[a-f0-9][a-f0-9][a-f0-9][a-f0-9][a-f0-9][a-f0-9][a-f0-9][a-f0-9]-*.json
df0afd2f-*.json
# Cloudflare config that contains tunnel ID
cloudflared-config.yml
cloudflared/

# Build artifacts
*.tsbuildinfo

# Internal documents — not part of source code
SECURITY_AUDIT_*.md
SECURITY_AUDIT_*.html
MVP-REPORT.html
MVP-REPORT-*.html
docs/plans/
docs/qa-security-report-*.md
docs/recipe-book-promo.*
progress.md

# Security scan outputs
trufflehog-report.json
gitleaks-report.json

# Environment variations — prevent any local env from leaking
.env.production
.env.development
.env.test

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Dependencies — safety net
node_modules/

# Deploy ops — keep local only (public repo safety)
.github/workflows/dev.yml
.github/workflows/prod.yml
scripts/deploy-dev.sh
scripts/deploy-prod.sh
scripts/start-runner.sh
nginx/default.conf.nossl

# Build
dist/
build/
